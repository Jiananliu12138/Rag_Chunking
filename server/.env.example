# ── 应用 ──────────────────────────────────────────────────────────────────────
DEBUG=false
LOG_LEVEL=INFO

# ── 路径 / Milvus 存储 ───────────────────────────────────────────────────────
# MILVUS_DATA_DIR 非空 → 使用本地 Milvus Lite，数据文件位于该目录下（<collection>.db）
# MILVUS_DATA_DIR 为空且配置 MILVUS_URI → 使用在线 Milvus / Milvus Server
# 两者都为空 → 回退到默认 Lite 目录（./milvus_data）
MILVUS_DATA_DIR=
MILVUS_URI=

# tiktoken 本地缓存目录（可选）
TIKTOKEN_CACHE_DIR=

# 分块任务默认并行度（可选，未配置时各模块使用各自的 NUM_WORKERS 默认值）
CHUNK_NUM_WORKERS=4

# ── 嵌入模型默认配置 ──────────────────────────────────────────────────────────
DEFAULT_EMBEDDING_MODEL=/path/to/bge-large-en-v1.5
DEFAULT_EMBEDDING_DIM=1024

# ── LLM 默认配置 ─────────────────────────────────────────────────────────────
DEFAULT_VLLM_API_BASE=http://localhost:8005/v1
DEFAULT_VLLM_MODEL_NAME=/path/to/Qwen2.5-7B-Instruct
DEFAULT_LLM_TEMPERATURE=0.1
DEFAULT_LLM_MAX_TOKENS=1280

# ── 评估默认配置 ─────────────────────────────────────────────────────────────
DEFAULT_ENABLE_BERT_SCORE=false
DEFAULT_BERT_SCORE_MODEL=roberta-large
DEFAULT_BERT_SCORE_DEVICE=cuda:0

# ── 组件级评估默认配置 ───────────────────────────────────────────────────────
# Chunk 质量评估（BC + 语义不相似度）
COMPONENT_PPL_MODEL_PATH=/path/to/internlm3-8b-instruct
COMPONENT_SIM_MODEL_PATH=BAAI/all-MiniLM-L6-v2
COMPONENT_ENABLE_SEMANTIC_SIMILARITY=true
COMPONENT_ENABLE_BOUNDARY_CLARITY=true

# Chunk 黏连度评估（结构熵）
STICKINESS_MODEL_PATH=/path/to/internlm3-8b-instruct
STICKINESS_THRESHOLD=0.8
STICKINESS_DELTA=0.0
